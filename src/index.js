import * as Objects from './lib/objects';
import * as Resources from './lib/resources';

export default {
  API: {
    Object: {
      ...Objects
    },
    Resource: {
      ...Resources
    }
  }
};
